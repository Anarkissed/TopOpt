{
  "export_frame": {
    "baked": true,
    "build_direction_in_file": [
      0,
      0,
      1
    ],
    "applied_build_dir_model": [
      0,
      0,
      1
    ],
    "auto_applied": true,
    "rotation_exact": true,
    "note": "the exported mesh was ROTATED so the certified build direction is +Z in the file. fields.bin, the voxel grid, the loads, the fixtures and the clearances are all in the MODEL frame and did NOT move. Every direction-bearing NUMBER in the report is a rigid-motion invariant and reads the same in both frames."
  }
}