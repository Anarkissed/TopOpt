# Lattice mode UI — evidence (2026-07-29)

Generated by `LatticeModeEvidenceGen` (opt-in:
`TOPOPT_LATTICE_EVIDENCE_DIR=<this dir> swift test --filter LatticeModeEvidenceGen`).
See `docs/handoffs/2026-07-29-lattice-mode-ui.md`.

- **`certifiable_band_from_core.txt`** — the certifiable band + certifiable-topology set
  read from CORE at runtime via `topoptbridge.lattice_limits` (octet = `[0.14764, 0.59093]`,
  the rest preview-only). Proof for U2 that the bounds come from core, not app constants.
- **`job_off_vs_on.txt`** — the lattice-OFF and lattice-ON `job.json`: the ONLY difference is
  the added `lattice` block (ON minus the block == OFF). Proof for U1 (byte-identical off).
- **`clamp_reasons.txt`** — `LatticeBounds` reasons across scenarios (out-of-band density,
  preview-only topology, and a too-coarse cell IF core certified a cells-per-member ceiling).
  Proof for the ★ "say why a control is clamped" bar.
- **`topology_samples/*.png`** — the true-geometry sample rendered per topology through the
  proxy's `LatticeSamplePatch` path — what the picker shows so a user can choose on looks.

**Owed (U7):** a live iPad screenshot of the controls panel + the region gizmo on the
maintainer's own part — the on-device step, per the PR-229 precedent. Nothing on the Mac
substitutes for the on-device frame; these component artifacts are the comparable proxy.
