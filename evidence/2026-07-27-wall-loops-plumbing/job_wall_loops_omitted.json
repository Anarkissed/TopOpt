{
  "_comment": "W1 CONTRAST job: identical to wall_loops_job.json but the loads block OMITS wall_loops — exactly what the app used to emit. The CLI defaults loads.wall_loops to 0, so run_info reads wall_loops: 0 (the bug's symptom).",
  "model": "l-bracket.step",
  "material": "PLA",
  "mode": "minimize_plastic",
  "resolution": 16,
  "loads": {
    "anchors": [ { "kind": "cylindrical", "radius_mm": 2.5 } ],
    "minimize_plastic": true,
    "build_dir": [0.0, 0.0, 1.0],
    "infill_percent": 20
  },
  "simp": { "max_iterations": 4 },
  "output": { "report": "report.json", "mesh_format": "stl", "mesh_prefix": "variant" }
}
