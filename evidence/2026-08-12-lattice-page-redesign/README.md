# evidence — 2026-08-12-lattice-page-redesign

Handoff: `docs/handoffs/2026-08-12-lattice-page-redesign.md`

| file | what it is |
|---|---|
| `M2_verticalStand.step` | THE MAINTAINER'S OWN PART, the same file PR 293 captured — so §0's arms are reproducible. |
| `s0_depth/job_mismatch.json` | ★ ARM A — HIS RUN AS CONFIGURED. His captured job (`evidence/2026-08-04-protect-freeze-vs-solidity/job_maintainer.json`, unmodified except the model path) with the include face regions dragged to **7 mm** and the protection left at the **5 mm** global. The defect, stated as a document. |
| `s0_depth/job_matched.json` | ★ ARM B — THE DEPTH FIX. The same job with the protection carried per face in the NEW object form, `[{"face_id": 16, "depth_mm": 7}]`, so the barrier is exactly as deep as the lattice it feeds. |
| `s0_depth/job_auto.json` | ★ ARM C — WHAT "AUTO" NOW EMITS. Arm B plus `"cell_mode": "fit"` — the per-region cell **core's own pre-flight names** in arm A's log. This is the arm R1 is about. |
| `s0_depth/{mismatch,matched,auto}.log` | the three runs. Arm A's log carries core's pre-flight table, which is the §0(c) "bigger finding". |
| *(the binary)* | all three arms ran one snapshot build, fingerprint **`c618a90a3a72`** (`topopt-cli --version`). It is not committed (2.7 MB); the fingerprint is how a rebuild is checked rather than trusted. |
| `s0_depth/s0_table.txt` | ★ **THE §0 / R1 TABLE**, generated by `s0_table.py`: protection depth, effective depth, voxels held, latticed voxels, solid fallback, cell, mass, and the include-on-void count — all three arms side by side. |
| `s0_table.py` | the generator. Every figure is READ from a receipt; where a receipt carries none, the cell prints `—` rather than a number someone computed. |
| `r1f_receipt_matches_log.sh` | ★ **§1f** — runs the CLI and requires `loadcase.json`'s `anchor_pad` to agree with the `[loadcase] anchor-pad` log line field for field, and the declared protections to be exactly what was declared at the per-face depth. There is no unit seam (`loadcase_receipt_json` is file-static), and this caught the receipt saying 0 while the log said 32,648. |
| `r7/receipt_matches_log.txt` | its output. PASS. |
| `s0_depth/out_*/` | each arm's committed receipts — `loadcase.json`, `run_info.json`, `variant_068_lattice.report.json`. **The meshes and fields are NOT committed** (see `.gitignore`): arm C's rung-0.68 lattice STL alone is 755 MB, which is the maintainer's own file-size complaint. Regenerate with the commands below. |
| `r4_preview_latency.txt` | ★ R4 — measured preview latency per interaction, median of 9 builds, RELEASE, on an M2 Pro. Generated by `LatticeWizardLatencyEvidence`. |
| `r6/byte_identity.txt` | ★ R6 — the shipped SIMP path, base (`c618a90`) vs this branch, sha256 per artifact, plus the verdict paragraph reading it against R6 as stated. |
| `r7/assertion_census.txt` | ★ R7 — assertion counts by kind, base vs branch. Nothing weakened, nothing deleted. |
| `l5/failing_first_compile.txt` | ★ R5 — the L5 test failing FIRST: `LoadCaseTagger.apply` had no way to express "this group's declaration is a lattice role", so the test could not even compile against it. |
| `app_tests.txt` | the full app suite. |
| `ctest.txt` | the full core suite on this machine. **Report N/<CI's total>** — this configuration registers fewer tests than CI. |

## Reproduce

    # core
    cmake -S core -B build -DCMAKE_BUILD_TYPE=Release
    cmake --build build --target topopt_cli test_lattice_depth_tie
    ./build/test_lattice_depth_tie

    # the three arms (each walks the production 4-rung ladder at resolution 128)
    E=evidence/2026-08-12-lattice-page-redesign/s0_depth
    for arm in mismatch matched auto; do
      ./build/topopt-cli run $E/job_$arm.json --out $E/out_$arm --no-iteration-csv
    done
    python3 evidence/2026-08-12-lattice-page-redesign/s0_table.py

    # §1f — the receipt must say what the log says
    DEMO_DIR=core/tests/fixtures/demo \
      evidence/2026-08-12-lattice-page-redesign/r1f_receipt_matches_log.sh \
      "$PWD/build/topopt-cli" /tmp/r1f

    # app
    swift test --package-path app/TopOptKit --filter "LatticeBarrierModelTests|LatticeWizardTests"
    swift test --package-path app/TopOptKit -c release --filter LatticeWizardLatency   # rewrites r4_preview_latency.txt

## The result, in one line

Arm A reproduces his run (include-on-void 120,726 vs his 120,821). Arm B fixes the
depth and the part gets HEAVIER — the barrier delivers 24.9% more material and all
of it falls back to solid at a cell too coarse to certify it. Arm C adds the
per-region cell core itself prescribes and the same 16,009 voxels lattice
completely: **+22.8% latticed voxels on his 13,034, 13.0% of the part vs his 12%,
490.6 g vs his 507 g** (solid 543.7 g).

## Two things the arms had to be told, by core

1. **`"skin": "rim"` emits nothing on this part**, and core refuses rather than
   export an undressed lattice under a finish the job asked for. A pre-existing
   gap (handoff 2026-08-05-lattice-cell-size-adaptation §7), not this task's — all
   three arms carry `"skin": "none"`, which is what core itself instructs.
2. **A custom `ladder` is not allowed alongside `loads`** — in loadcase mode the
   production ladder and margin apply. So every arm walks the full 4-rung ladder;
   there is no short version of this measurement.
