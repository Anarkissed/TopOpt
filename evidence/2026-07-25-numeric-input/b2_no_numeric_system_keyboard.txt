B2 — no path raises the full system keyboard for a NUMBER (demonstration)
Generated by grep over app/TopOptKit/Sources/TopOptFlows/

$ grep -rn "keyboardType(\." .
ComputeLocationControl.swift:98:                            .keyboardType(.URL)

  → the ONLY .keyboardType left is .URL (the manual HOST text field, not a number).
    Every .decimalPad / .numberPad on a numeric field is gone.

$ grep -rn "\.keyboardType(\.decimalPad)\|\.keyboardType(\.numberPad)" .   # expect NONE
  (no matches)

$ grep -rn "TextField(" .    # remaining TextFields are all TEXT, never numbers
ComputeLocationControl.swift:94:                        TextField("Host or IP", text: $manualHost)
HomeView.swift:50:            TextField("Name", text: $renameDraft)
PrintParamsSheet.swift:69:            TextField("Preset name", text: $presetName)
WorkspacePlaceholder.swift:588:            TextField("Name", text: $nameDraft)
WorkspacePlaceholder.swift:1628:                TextField("Group", text: binding(for: g))

  → Host/IP (URL), Preset name, project Name x2, Group name — all text fields.
    Every numeric field now opens the in-app NumberPad popover instead.
