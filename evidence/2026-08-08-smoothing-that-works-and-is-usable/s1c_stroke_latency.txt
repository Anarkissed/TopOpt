STROKE LATENCY ON HIS OWN VARIANT — rung 068

BUILD: RELEASE — the configuration the app ships.

mesh   variant_068.stl: 14386084 bytes, 143862 vertices / 287720 triangles
brush  every vertex at weight 1.0, strength 0.49 (Taubin pairs as resolved by taubin_params_for_strength)
repeats per row: 5; every figure is ms PER STROKE

stage                                  repeats    ms/stroke
------------------------------------------------------------
A  preview via PATH   — STL re-import        5     588.0
A  preview via PATH   — smoothing            5      35.5
A  preview via PATH   — TOTAL                5     628.8
B  preview via MEMORY — STL re-import        5       0.0
B  preview via MEMORY — smoothing            5      34.9
B  preview via MEMORY — TOTAL                5      43.7
   ViewerMesh build (app side, both)         5      19.7
------------------------------------------------------------
stroke release -> updated preview, BEFORE:    648.4 ms
stroke release -> updated preview, AFTER :     63.3 ms
removed: 585.1 ms per stroke (90.2% of the before figure)


S1d — WHAT ELSE A PASS REBUILDS (not on the critical path above, but
      paid on EVERY SwiftUI body evaluation, of which a stroke causes
      several):
   smoothBrush.viewerTints()  863160 entries       11.8 ms
   the coordinator's array compare against the uploaded copy      0.6 ms

The import share of the SHIPPED preview call: 94.3%
moved vertices 143862 / 143862; max displacement 0.4449 mm
