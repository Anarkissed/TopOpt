★ R12 — ASSERTION CENSUS vs THE MERGE BASE d28527cc.
Read WHOLE, not through a filter: every removed line is listed below, and the
assertion-bearing ones are counted out of that complete list.

--- every removed line in core/ (18 in total) ---
     1	                         "outer_finish", "regions", "multiscale",
     2	             "subfloor_per_region"},
     3	    double base_cell_mm = 0.0, double printed_iso = 0.5) {
     4	    if (!measure_protrusion)
     5	                                                 Rbase, *levels, w, skin)
     6	                                      skin);
     7	    obs.on_element = [&m, &pass_marks](LatticeGenElement k, const Vec3&,
     8	                                       const Vec3&, double) {
     9	        swept ? generate_lattice_multilevel(LatticeGenTopology::Octet, Rbase,
    10	                                            *levels, m, skin, &obs)
    11	              : generate_lattice(LatticeGenTopology::Octet, R, radius, m, skin,
    12	                                 &obs);
    13	    (void)region_ids;
    14	      levels.empty() ? 0.0 : gf.cell_plan.base_cell_mm, printed_iso);
    15	void emit_node(TriangleSink& sink, const Vec3& c, double r) {
    16	  return 0.5 * nseg * r * r * std::sin(2.0 * M_PI / nseg) * len;
    17	  const double a = 4.0 * circumradius / std::sqrt(10.0 + 2.0 * std::sqrt(5.0));
    18	  return (5.0 / 12.0) * (3.0 + std::sqrt(5.0)) * a * a * a;

removed lines carrying throw / assert / a refusal:   0
assertion sites ADDED by this task:                 28
test files touched:                                  0   (none deleted, none weakened)

Every removed line above is a MOVE or a replacement in kind:
  * emit_strut / emit_node moved from the anonymous namespace to public linkage so the
    ORGANIC generator emits the SAME swept solids rather than a second copy of them.
    Bodies unchanged; the octet output stays byte-identical, which R2 proves.
  * ngon_prism_volume / icosahedron_volume now delegate to the public arithmetic — one
    implementation of each volume, not two that can drift.
  * the emit_lattice dispatch and the observer lambda grew cases for stepped, organic
    and the weld span ledger.
  * (void)region_ids became a real use of the same variable.

★ ONE CHANGE A REVIEWER SHOULD LOOK AT DELIBERATELY, because it is the only one that
alters behaviour on a path this task was told not to touch:

  `lattice_one_variant` now passes the grading INTENT and the aesthetic range into
  GradingLawParams. Before this, a job stating `intent: "aesthetic"` and a density range
  had both SILENTLY IGNORED on the path that writes geometry — PR 344/345 wired them
  into `analyze_job` only — and rho ran over the whole certifiable band instead.

  It is guarded by `if (!job.grading.intent.empty())`, so a job stating no intent keeps
  the Structural default and two zero bounds (which grading.hpp documents as "the
  certifiable band"). ★ THAT IS MEASURED, NOT ARGUED: the second arm of
  r2_doubled_byte_identity.txt runs `lattice-variant` with no intent stated, on the BASE
  binary and on this one, and the emitted `variant_070_lattice.stl` is BYTE-IDENTICAL.
  The TO+lattice path states no intent, so it cannot acquire one.
