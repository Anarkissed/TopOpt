=== R7 assertion census: main -> working tree ===

1. TEST ASSERTION MESSAGES   before=3304  after=3304
   REMOVED:
   (nothing above = no assertion message disappeared)

2. REGISTERED CTESTS   before=119  after=119
   REMOVED:
   ADDED:

3. PRODUCTION REFUSALS   before=409  after=409
   REMOVED:
   (nothing above = no production refusal disappeared)

4. COMPARISON KINDS INSIDE CHECK() (before -> after; a DROP is the thing to look at)
     !=         31 ->     31  
     &&        282 ->    282  
     <         221 ->    221  
     <=        136 ->    136  
     ==        829 ->    829  
     >         254 ->    254  
     >=         60 ->     60  
     ||          2 ->      2  

5. HARNESS MESSAGES (the whole of core/tests/harness as ONE bag)
   before=6177  after=6307
   REMOVED:
     "lattice; %s; %zu coefficients"
   (nothing above = the two header moves dropped no refusal and no message)

6. THE REMOVED HARNESS STRINGS, TRIAGED
     EXTENDED (a superset is still present): "lattice; %s; %zu coefficients"
