R1 — BYTE-IDENTICAL WHEN OFF, by stash-rebuild checksum.

This task adds NO production source. The claim is proven the way PR 299
proved it: two FRESH build directories, configured with identical flags,
one from the working tree and one from origin/main, and every object file
of the shipped library digested. Not 'by construction'.

== the diff against origin/main, restricted to shipped source ==
$ git diff origin/main -- core/src core/include app/TopOptKit/Sources | wc -l
       0

== everything this branch touches ==
    M evidence/2026-08-05-smoothing-sdf-geometry-extraction/r1_byte_identity.txt

== build A: the working tree ==
objects: 45

== build B: origin/main, from a clean checkout of the same tree ==
objects: 45

== the digests ==
digest of all objects, working tree      : 565a792e4f6791b10f65f271c0ed6f59
digest of all objects, origin/main       : 565a792e4f6791b10f65f271c0ed6f59
BYTE-IDENTICAL

(The .a archives are NOT compared: ar embeds member mtimes, so two
 archives of identical objects differ in bytes that describe when they
 were written rather than what they contain. PR 299 made the same choice
 for the same reason, and checked it by extracting both and diffing every
 member. Same check here:)
extracted archive members: NO BYTE DIFFERS

== the app library, which this task also does not touch ==
$ git diff origin/main --stat -- app/

======================================================================
RUN TWICE, AGAINST TWO DIFFERENT MAINS.

`main` moved during this task (#298 lattice cell-size adaptation, #299 the
predecessor whose harness was cherry-picked, #300 smoothing-page brush panel),
so this bar was measured before and after merging it up.

  against origin/main = b3abcf8   both sides: 47669432db84403491cedb838d3f46f0
  against origin/main = 90e9ec5   both sides: 565a792e4f6791b10f65f271c0ed6f59

The first digest is the one PR 299 recorded for the same commit
(evidence/2026-08-05-smoothing-must-actually-smooth/r1_byte_identity.txt). The
digest MOVED between the two runs because MAIN moved — #298 alone added 1,022
lines across core/src/fea/lattice.cpp, core/src/cli/run_job.cpp and
core/include/topopt/job.hpp. What did not move, either time, is the agreement
between the two sides of the comparison: this branch adds no production source.

The block above is the second (current) run, verbatim.
