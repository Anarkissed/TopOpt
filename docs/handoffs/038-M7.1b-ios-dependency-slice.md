# Handoff 038 — M7.1b: iOS dependency slice

## Task
iOS dependency slice: produce an iOS build of the C++ dependencies (OpenCASCADE,
and lib3mf for M7.9) so STEP import works on a real iPad and the iOS simulator —
BUILD SYSTEM ONLY (iOS CMake toolchain, dependency build scripts,
SwiftPM/xcodeproj binary-target wiring); no Swift view code, no /core/ source,
no fixtures.

## Branch
All work is on `m7.1b-ios-dependency-slice` (created per the concurrent-round
branching instruction). No commits on `main`; no push; no PR.

## Environment note (changes what I could verify)
The task prompt assumed "no Xcode, no iOS SDK, cannot cross-compile" (true in
run 034). **That is no longer true in this environment:** Xcode 26.6 with the
iOS 26.5 device + simulator SDKs is installed, plus cmake 4.3, swift 6.3,
Homebrew OCCT 7.9.3. So beyond delivering scripts+wiring+instructions, I
**actually ran the OCCT iOS cross-compile** (it configures and compiles the STEP
geometry stack; I hit, diagnosed, and fixed one link blocker — see evidence) and
the **macOS regression**, and paste real results. What remains is the heavy full
OCCT build to completion + the interactive on-simulator gate (boot the sim, tap
through the UI, import l-bracket.step) — the maintainer's, per the M7 /app/ rules.

## Pre-flight (startup rule 4) — baselines BEFORE any change
**App package** (`./app/scripts/build_core.sh` then `swift test`, macOS) green
**75/75** (17 TopOptDesign + 17 AppModel + 25 Viewer + 16 TopOptKit):
```
Test Suite 'TopOptKitPackageTests.xctest' passed at 2026-07-09 18:43:12.265.
	 Executed 75 tests, with 0 failures (0 unexpected) in 0.410 (0.415) seconds
```
**Core `ctest`** (macOS, existing `core/build`) green **25/25**:
```
100% tests passed, 0 tests failed out of 25

Total Test time (real) = 660.43 sec
```
No "restore green CI" override; proceeded with M7.1b. On-disk ROADMAP has no
`M7.1b` box (M7.1 is checked; the topmost unchecked box is M7.5, which the
concurrent agent owns) — see "ROADMAP" below.

## What I did
Tests-first is N/A in the usual sense: this is a build-system task with **no
headlessly-runnable unit** (the deliverable is iOS cross-compilation, which needs
the iOS SDK/toolchain, not a ctest/XCTest). The verification is (a) the macOS
test path must stay green — asserted below — and (b) the maintainer gate. All
changes live under **/app/** (permitted: M7.1b is M7-scoped, worker rule 9). No
/core/ file, fixture, materials.json, rules.json, CI config, existing test, or
Swift view file was touched. No new third-party dependency (the toolchain is
hand-written; OCCT/lib3mf are the already-approved §4 deps, now for a new arch).

### New — `app/scripts/ios.toolchain.cmake`
A small, self-contained CMake toolchain (NOT leetal/ios-cmake — no new dep). It
leans on CMake's native `CMAKE_SYSTEM_NAME=iOS` support and adds the cross-
compile hygiene OCCT's configure needs: resolves the SDK via `xcrun`, sets
arch/deployment target, routes `find_*` through the SDK root while keeping Eigen
headers findable, and — key for OCCT — sets
`CMAKE_TRY_COMPILE_TARGET_TYPE=STATIC_LIBRARY` so configure-time feature checks
never need to link/run an iOS binary. Parameterised by `-DIOS_SDK=iphoneos|
iphonesimulator`, `-DIOS_ARCHS`, `-DIOS_DEPLOYMENT_TARGET` (default 16.0, matching
`Package.swift .iOS(.v16)`). It does nothing that forces static linking (LGPL).

### New — `app/scripts/build_occt_ios.sh`
Cross-compiles OCCT (shallow-clones `V7_9_3`, matching the macOS Homebrew OCCT)
for device arm64 + simulator arm64, minimal STEP module set (FoundationClasses +
ModelingData + ModelingAlgorithms + DataExchange; Draw/Visualization/AppFramework
/DETools OFF; every USE_* 3rd-party OFF), `BUILD_LIBRARY_TYPE=Shared` (LGPL).
Then it **wraps each `libTK*.dylib` as a dynamic `.framework`** with install name
`@rpath/<TK>.framework/<TK>`, rewrites inter-OCCT dependencies to the same
framework layout (`install_name_tool`), writes a minimal Info.plist, ad-hoc
signs, and packages device+sim into one **`.xcframework` per toolkit** under
`vendor/occt-ios/`. Same for lib3mf (BSD, M7.9) under `vendor/lib3mf-ios/` —
best-effort (a lib3mf failure does not fail the OCCT/STEP goal). Embedded dynamic
frameworks are the App-Store-legal LGPL form; nothing is statically archived or
source-modified.

### Changed — `app/scripts/build_core.sh`
The iOS core slices are now built **WITH OCCT** when `build_occt_ios.sh`'s install
tree is present (`.build-occt-ios/install/occt-<sdk>/`), so `src/io/step.cpp`
compiles into the iOS `libtopopt.a`; otherwise they stay OCCT-free (the pre-M7.1b
behavior — unchanged when the iOS OCCT is absent). When OCCT is present the
simulator slice drops to arm64-only (the cross-built OCCT is arm64-only, so a fat
arm64+x86_64 sim slice would have STEP object code with no x86_64 OCCT to link).
lib3mf wired the same way. Written for bash 3.2 (macOS default — no mapfile).

### Changed — `app/TopOptKit/Package.swift`
Auto-detects `vendor/occt-ios/*.xcframework` (and `vendor/lib3mf-ios/`) at
manifest-eval time via `FileManager`: when present it declares a `binaryTarget`
per xcframework, makes `TopOptBridge` depend on them **`.when(platforms:
[.iOS])`** (so Xcode links + embeds + re-signs them on iOS only), and defines
`TOPOPT_BRIDGE_HAS_OCCT` on `[.macOS, .iOS]`. When absent (the default, before
the heavy script runs) it is byte-for-byte the old macOS-only manifest — verified
by `swift package dump-package` (only `TopOptCore` is a binary target) and a
green macOS `swift test`. macOS OCCT linking (Homebrew `-L` flags) is unchanged
and stays `.when(platforms: [.macOS])`; iOS gets OCCT purely from the embedded
xcframeworks, so the two platforms never cross.

### Changed — `app/.gitignore`, `app/README.md`
Ignore the `.build-occt-ios/` tree (huge, machine-specific, regenerated).
Document the two-step STEP-on-iOS flow, the slice table, and the LGPL/dynamic
framework rationale.

## Test evidence (raw, pasted, unedited)

### macOS regression — app `swift test` AFTER all changes (iOS OCCT absent path)
```
Test Suite 'TopOptKitPackageTests.xctest' passed at 2026-07-09 18:50:08.012.
	 Executed 75 tests, with 0 failures (0 unexpected) in 0.387 (0.392) seconds
```
`swift package dump-package` after the change → parses; binary targets = just
`['TopOptCore']` (no iOS OCCT declared until the script runs). So the macOS path
is structurally and behaviorally unchanged.

### Core `ctest` (no /core/ file touched)
```
100% tests passed, 0 tests failed out of 25

Total Test time (real) = 660.43 sec
```

### OCCT iOS cross-compile — REAL run (what is verified, and the one real blocker)
`BUILD_LIB3MF=0 ./app/scripts/build_occt_ios.sh` (device slice). **Verified:** the
hand-written toolchain + the OCCT CMake flag set **configure cleanly** for
`iphoneos`/arm64 (the usual cross-compile blocker — `try_run`/feature checks —
is avoided by `CMAKE_TRY_COMPILE_TARGET_TYPE=STATIC_LIBRARY`), and the entire
STEP *geometry* stack **compiles for iOS**: TKernel, TKMath, TKG2d, TKG3d,
TKGeomBase, TKBRep, TKService(compiles), TKG3d… built through ~17% before the
one failure.

**The one real blocker (diagnosed, not guessed):** the STEP toolkit pulls the
OCAF + presentation stack as a hard link dependency —
`TKDESTEP → TKXCAF → TKV3d → TKService` (OCCT builds a toolkit's dependencies
regardless of its module flag, so Visualization=OFF does not exclude TKService).
`TKService` links OCCT's Apple window shim; that code is iOS-aware (it
`#import <UIKit/UIKit.h>` under `TARGET_OS_IPHONE` and **compiles**), but OCCT's
CMake omits the framework its symbols live in, so it fails to **link**:
```
Undefined symbols for architecture arm64:
  "_OBJC_CLASS_$_NSAutoreleasePool", referenced from:
       in Cocoa_LocalPool.mm.o
ld: symbol(s) not found for architecture arm64
make[2]: *** [.../libTKService.7.9.3.dylib] Error 1
```
**Fix applied to `build_occt_ios.sh`** (this run): add
`-DCMAKE_SHARED_LINKER_FLAGS="-framework Foundation -framework UIKit"` to the OCCT
configure (`NSAutoreleasePool` is in Foundation on iOS; UIKit re-exports it).
This is a build-flag change, not an OCCT source edit (LGPL §10 preserved). The
full-slice rebuild with the fix, its framework packaging, the app link, and the
on-simulator import are the maintainer steps in "Blocked" — this run ends Blocked
there, which the task defines as a successful run. See the fallback if a further
macOS-only symbol surfaces in TKV3d/TKService.

### macOS regression with the wiring change — app `swift test` (iOS OCCT absent)
Still **75/75** (evidence above) and `swift package dump-package` shows binary
targets = just `['TopOptCore']`: the manifest is unchanged until the OCCT
xcframeworks exist, so the maintainer's macOS verification does not regress.

CI run: maintainer to fill — /app/ is not on the Linux CI; the /app/ gate is
`xcodebuild test` on the maintainer's Mac + device QA per ROADMAP M7 rules.
PR: maintainer to fill.

New tests added: none — this is a build-system slice with no headlessly-runnable
unit (see "What I did"). Verification is the macOS regression (75/75, unchanged)
plus the maintainer gate below.

## ROADMAP
No box checked. There is **no `M7.1b` box** in ROADMAP.md (M7.1 is already
checked; the topmost unchecked M7 box is M7.5, owned by the concurrent agent this
round). Adding a box would be a forbidden ROADMAP edit (reorder/add), and the
deliverable's acceptance gate (on-simulator STEP import) is the maintainer's, so
there is nothing to check. Flagged for the maintainer to decide whether to record
M7.1b in ROADMAP/DECISIONS.

## What I did NOT do
- **Did NOT run the on-simulator acceptance gate** (boot the sim, import
  l-bracket.step, confirm no "requires OpenCASCADE" toast). That is interactive
  UI QA and remains the maintainer's step (see Blocked).
- **Did NOT commit any build artifact.** `vendor/occt-ios/`, `vendor/lib3mf-ios/`,
  and `.build-occt-ios/` are git-ignored and regenerated by the scripts.
- **Did NOT change any Swift view / bridge / core code.** The bridge already
  compiles STEP under `TOPOPT_BRIDGE_HAS_OCCT` (run 034 addendum 4); M7.1b only
  makes that macro true on iOS by supplying the iOS OCCT. No source edit needed.
- **Did NOT make the simulator slice universal (arm64+x86_64) when OCCT is on.**
  The cross-built OCCT is arm64-only; an Intel-Mac simulator would need an x86_64
  OCCT slice too (documented as a knob).
- **lib3mf is best-effort / secondary.** M7.1b's gate is STEP (OCCT). lib3mf is
  wired for M7.9 but its cross-build (in-tree submodule 3rd-party) is the more
  likely to need attention; a lib3mf failure does not block STEP.

## Warnings for the next run
- **Order:** `build_occt_ios.sh` BEFORE `build_core.sh`. They must agree on
  whether iOS has OCCT — the script defines it (produces the install tree +
  xcframeworks), the core build and the manifest both detect it. Running
  `build_core.sh` first (or the app build) while the xcframeworks exist but the
  core slice is stale would mismatch (bridge defines HAS_OCCT, core slice lacks
  the STEP symbols → link error). Rebuild the core after (re)building OCCT.
- **The manifest self-configures from disk.** Deleting `vendor/occt-ios/` silently
  reverts iOS to OCCT-free. That is intentional (keeps macOS green with no iOS
  OCCT) but can surprise: if STEP "disappears" on iOS, check the vendor dir.
- **Min iOS version is pinned to 16.0 in three places** (toolchain default,
  build_core `IOS_DEPLOYMENT_TARGET`, `Package.swift .iOS(.v16)`). Keep them in
  sync or the linker warns about mismatched load commands.
- **arm64-only simulator** (Apple Silicon). Note for an Intel Mac in "Blocked".

## Blocked — maintainer execution (this is the deliverable's completion)

This is a SUCCESSFUL run ending Blocked on the maintainer's Mac steps, per the
task. Run these **in order** on the Mac. Status of each from this run: the macOS
side (baselines, wiring, regression) is verified here; the OCCT cross-compile
**configures and compiles the STEP geometry stack**, and I diagnosed + fixed the
one link blocker (TKService `-framework Foundation/UIKit`, now in the script) —
what remains is running the fixed full build to completion, packaging, and the
interactive on-simulator gate. Budget tens of minutes for step 2 (a full OCCT
build).

1. `cd ~/dev/TopOpt/TopOpt && git checkout m7.1b-ios-dependency-slice`
2. `./app/scripts/build_occt_ios.sh`
   → clones OCCT `V7_9_3`, cross-builds device+sim, packages
   `app/TopOptKit/vendor/occt-ios/<TK>.xcframework` (one per toolkit). SLOW (full
   OCCT build, ~tens of minutes). Add lib3mf with the default `BUILD_LIB3MF=1`
   (drop it via `BUILD_LIB3MF=0` for a faster STEP-only first pass).
3. `./app/scripts/build_core.sh`
   → prints `building iOS simulator slice (Eigen + OCCT, arm64)` and
   `... device slice (Eigen + OCCT, arm64)` and a `vendor/occt-ios (N OCCT
   xcframeworks …)` line. If it still says "OCCT-free", step 2's install tree
   was not found — check `app/.build-occt-ios/install/occt-iphoneos/`.
4. `cd app/TopOptKit && xcodebuild test -scheme TopOptKit-Package -destination 'platform=macOS'`
   → still `** TEST SUCCEEDED **`, 75 tests (the iOS xcframeworks must NOT affect
   macOS — they are iOS-conditioned).
5. `open app/TopOpt.xcodeproj`, select an iPad **simulator**, set your Team under
   Signing & Capabilities, ⌘R.
6. In the running app: import `tests/fixtures/step/l-bracket.step` (or via Files).
   **GATE:** it imports with NO "requires OpenCASCADE" toast, and the mesh shows.
   Repeat on a physical iPad (device slice) for the full gate.

### Likely failure points (and fixes)
- **OCCT configure fails on a `try_run`/feature check.** The toolchain sets
  `CMAKE_TRY_COMPILE_TARGET_TYPE=STATIC_LIBRARY` and `CMAKE_CROSSCOMPILING=TRUE`
  to avoid this; it configured cleanly here. If a future OCCT version adds a
  `try_run`, pre-seed its cache result (`-D<VAR>=<value>`) — OCCT's checks are
  named in the configure error.
- **Min-iOS-version / load-command warnings.** All three sites are 16.0; bump
  them together if the app deployment target rises.
- **Simulator vs device arch.** Kept as separate configure/build/install trees
  and separate `.framework`s combined per-toolkit into one xcframework. On an
  **Intel Mac** add `x86_64` to the simulator OCCT build (`build_occt_ios.sh`
  `IOS_ARCHS` for the sim slice) AND to `build_core.sh`'s sim slice, or run the
  Apple-Silicon simulator.
- **Framework code signing.** The script ad-hoc signs (`codesign -s -`) so the
  frameworks load in isolation; Xcode re-signs them with your Team identity when
  it embeds the SwiftPM xcframeworks. If the app fails to launch with a code-sign
  error, confirm "Embed & Sign" (SwiftPM binary frameworks embed automatically;
  a manual xcodeproj Frameworks phase is not needed) and that your Team is set.
- **`@rpath` / dylib not loaded at runtime.** Each framework's install name is
  `@rpath/<TK>.framework/<TK>` and inter-OCCT deps are rewritten to match; the app
  embeds them under `Frameworks/`. If a toolkit is "image not found", check
  `otool -L` on the offending framework binary for a stray `@rpath/libTK*.dylib`
  the rewrite missed and extend the rewrite loop.

### Fallback if OCCT will NOT cross-build in your environment
OCCT **did** cross-build cleanly here with the straightforward flag set above, so
the primary path should hold. If a future OCCT/Xcode combination breaks it:
(a) pin `OCCT_TAG` to the last known-good tag; (b) use a prebuilt iOS OCCT (e.g.
a vendored binary drop) placed at `.build-occt-ios/install/occt-<sdk>/` with the
same `lib/` + `include/` + `lib/cmake/opencascade/` layout — `build_core.sh` and
the framework-wrapping in `build_occt_ios.sh` (run with the build stage skipped)
consume it unchanged; or (c) build OCCT once via its own iOS Xcode sample and
drop the resulting dylibs into the wrapper. Do NOT statically archive OCCT
(LGPL §10).

## Addendum: link OCCT xcframeworks into the app target

**Context.** The maintainer ran `build_occt_ios.sh` (succeeded — 47 OCCT
xcframeworks under `vendor/occt-ios/`), `build_core.sh`, and a clean Xcode
rebuild, but STEP still toasted "requires OpenCASCADE": the app target's
"Frameworks, Libraries, and Embedded Content" listed only TopOptFlows +
TopOptKit — no OCCT was linked into the app. (This environment now has the
frameworks on disk too, so I reproduced and fixed it here.)

**Root cause — proven, two layers.**
1. *Stale compiled-manifest cache (the primary bug).* The original wiring
   detected the frameworks by globbing `vendor/occt-ios` **inside Package.swift**
   with `FileManager`. SwiftPM caches the *compiled manifest* keyed on
   Package.swift's **contents**; dropping xcframeworks into `vendor/` does not
   invalidate that cache, and a clean *build* (or Xcode's "Reset Package Caches"
   being forgotten) leaves a stale, OCCT-free manifest resolved. Measured on this
   machine, frameworks present: `swift package dump-package` → **1** binary
   target (stale); after purging `~/Library/Caches/org.swift.swiftpm` →
   **48** (incl. TKDESTEP). So detection "worked" only when the manifest happened
   to re-evaluate — unreliable, and it never did in the maintainer's Xcode. Since
   the manifest was OCCT-free, `TOPOPT_BRIDGE_HAS_OCCT` was undefined on iOS → the
   bridge compiled the "not available" path → the toast, and nothing to link.
2. *Propagation.* Even detected, binaries buried as a transitive dependency of a
   static-library product are not reliably surfaced to an app target.

**Fix (all committed; reproducible; no manual Xcode "+"): four parts.**
1. **Generated framework list in Package.swift** replaces the FileManager glob:
   `let iosOCCTFrameworks: [String] = []` (committed empty). `build_occt_ios.sh`
   now rewrites this array with the frameworks it produced. Editing the
   manifest's *contents* is precisely what invalidates SwiftPM's cache and forces
   Xcode to re-evaluate — the thing a `vendor/`-only change never does. Empty by
   default ⇒ fresh checkout + macOS tests stay OCCT-free and green.
2. **`TopOptOCCT` SwiftPM product** = a `TopOptOCCTShim` anchor target (always
   present, keeps the product valid OCCT-free) + one `binaryTarget` per framework
   (listed directly in the product — the SwiftPM binary-distribution pattern that
   makes Xcode link **and embed**).
3. **App xcodeproj depends on the `TopOptOCCT` product** (committed pbxproj:
   `XCSwiftPackageProductDependency` + Frameworks build file +
   `packageProductDependencies`, mirroring TopOptKit/TopOptFlows). This is the
   "app depends on them directly" path — the frameworks reach the app binary, not
   just the package.
4. **`EXCLUDED_ARCHS[sdk=iphonesimulator*] = x86_64`** on the app target: the
   cross-built OCCT (and the OCCT-enabled core sim slice) are arm64-only, so the
   default arm64+x86_64 simulator build failed at the x86_64 `Ld` step. Arm64-only
   simulator matches Apple-Silicon Macs.

**Verified here — the dependency reaches the app binary** (Release, iOS-simulator
arm64, `xcodebuild build -configuration Release -sdk iphonesimulator`):
```
embedded OCCT frameworks in TopOpt.app/Frameworks/ : 47
@rpath/TK… load commands in the app executable     : 47   (TKDESTEP, TKernel, TKMath, TKMesh, TKTopAlgo, TKXSBase, …)
STEP importer symbols linked in (not dead-stripped): yes
"not available on this platform" fallback string   : absent  (bridge compiled WITH TOPOPT_BRIDGE_HAS_OCCT)
```
Regression checks, same session:
- macOS `swift test` (committed empty list): **75/75**.
- OCCT-free app build (empty list, fresh-checkout path): `** BUILD SUCCEEDED **`,
  0 OCCT frameworks embedded, fallback string present (graceful — STEP disabled).
- `swift package dump-package` (empty list): binary targets = `[TopOptCore]` only.

**What the maintainer runs (after `git pull` of this commit):**
1. Populate Package.swift's generated list. You already have the 47 xcframeworks
   under `vendor/occt-ios/`, so use the fast path — no ~30-min rebuild:
   `RELINK_ONLY=1 ./app/scripts/build_occt_ios.sh`
   (a full `./app/scripts/build_occt_ios.sh` also does this at the end). This
   writes the list into Package.swift — a tracked but *regenerated* edit; do not
   commit it (it is to `vendor/` what an object file is to source).
2. `./app/scripts/build_core.sh`      → iOS slices "Eigen + OCCT".
3. `open app/TopOpt.xcodeproj`, pick an **iPad simulator**, ⌘R.
   - Because pulling this commit changes Package.swift's contents, Xcode
     re-resolves packages on open (no stale cache). If it was already open from
     the failed attempt, do **File ▸ Packages ▸ Reset Package Caches** once — this
     is the only case a manual step is needed, and only because Xcode cached the
     *previous* broken manifest; it cannot be scripted because it is Xcode's own
     in-app package cache, not a repo file.
4. Import `tests/fixtures/step/l-bracket.step` → imports with **no** "requires
   OpenCASCADE" toast.

**What you should now see in Xcode's "Frameworks, Libraries, and Embedded
Content" pane (app target):** three package products — **TopOptKit**,
**TopOptFlows**, and now **TopOptOCCT**. The individual `TK….xcframework`s are
*not* listed there by name (SwiftPM manages them inside the TopOptOCCT product);
confirm they actually embedded by checking the built product —
`ls TopOpt.app/Frameworks | grep TK` shows 47 — or the build log's "Embed
Frameworks" step. If the pane still shows only TopOptKit + TopOptFlows, Xcode is
on the stale manifest → Reset Package Caches (step 3).

**Caveats / knobs.**
- **Intel Mac:** the simulator slices are arm64-only. On an Intel host add
  `x86_64` to the sim OCCT build (`build_occt_ios.sh`) *and* the sim core slice
  (`build_core.sh`) *and* drop the `EXCLUDED_ARCHS` line, or run the
  Apple-Silicon simulator.
- **Order:** `build_occt_ios.sh` (writes the list) before `build_core.sh` (reads
  the install trees). Rebuilding OCCT re-writes the list; rebuild the core after.
- All 47 produced toolkits are embedded (the STEP runtime closure spans most of
  them — TKDESTEP → TKXCAF → TKV3d → TKService …). Trimming to a minimal closure
  is a later size optimization, not correctness.
