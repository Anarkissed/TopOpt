# Handoff 045 — M7.8: results-screen foundation (bridge + model + chrome + wiring)

## Task
M7.8 "Results screen. Variant tabs (−% labels + mass_grams from M7.0b), variant
mesh display with the morph/threshold scrub, stress overlay toggle (von Mises →
vertex colors, shared scale across variants), orientation sheet: M4.4
score_orientations result phrased per design, support estimate from
support_volume_voxels, 'Layer shear' = the variant's max interlayer tension."

## Scope of THIS run (maintainer-chosen: "foundation first")
M7.8 spans four layers: bridge/C++, a headless results model, SwiftUI chrome, and
Metal rendering. The Metal rendering (swap the stage to the variant mesh, stress
vertex-coloring, threshold-morph) is device-QA only — it can be compiled but not
visually verified by an agent. The maintainer chose to split it: **this run
delivers the fully-verifiable foundation** (bridge plumbing + headless-tested
`ResultsModel` + `ResultsScreen` chrome + success→results wiring), and the Metal
render pass is a follow-up. **The M7.8 box therefore stays UNCHECKED** until that
follow-up lands (worker rule: check a box only when the whole task is done).

Print-time decision: DECISIONS.md 2026-07-11 chose **(b) OMIT** print time in v1.
Handoff 044's initial state blocked M7.8 on this (no DECISIONS entry existed); the
maintainer added it, so M7.8 proceeded. The orientation sheet shows Layer shear
only (the design's Print-time field is omitted).

## Pre-flight (startup rule 4) — baseline BEFORE any change
- Core `ctest` (Linux-CI gate; no `/core/` file touched this run): **GREEN, 26/26**
  (`100% tests passed, 0 tests failed out of 26`, 598 s; cli_demo 419 s). Pasted
  below. Stands unchanged — the bridge is a SwiftPM target, not compiled by the
  core CMake build.
- App package (macOS, OCCT-free-iOS manifest per runs 039–044): 175 green before my
  change (handoff 044, Addendum 4); 194 after (below).
No "restore green CI" override.

## What I did
Tests first (rule 3), then implementation. All new/changed code is under
`app/TopOptKit/` (M7-scoped, worker rule 9). No `/core/`, `tests/fixtures/**`,
`materials.json`, `rules.json`, `ci/`, `ARCHITECTURE.md`, or `Package.swift` (M7.1c
invariant — only mtime touched for the manifest dance; content unchanged, not in
git status) was modified; no existing test assertion/tolerance changed. No new
third-party dependency (`simd`, `SwiftUI` ship with the OS). `<stdexcept>`
portability grep on the changed C++ files: empty (no new `std::` exception usage;
`bridge.cpp` already includes it).

The M4.4/M7.0b data the results screen needs is ALL already computed by the core on
each `MinimizePlasticVariant` (`report.orientation`, `report.max_stress_mpa`,
`report.max_interlayer_tension_mpa`, `report.margin.{in_plane,interlayer}`,
`support_volume_voxels`, `mass_grams`, `grid.voxel_volume()`). **No core work was
needed** — only bridge plumbing + Swift.

### Bridge (C++) — forward the scalar variant data the chrome/model consume
`Sources/TopOptBridge/include/TopOptBridge.hpp` + `bridge.cpp`:
- `OptimizeVariant` gains `orientation_{x,y,z}` (the M4.4 winning unit build
  direction), `max_stress_mpa`, `max_interlayer_tension_mpa`, `in_plane_margin`,
  `interlayer_margin` — populated from `v.report` in the existing evaluated-rung
  loop.
- `OptimizeResult` gains `voxel_volume_mm3 = grid.voxel_volume()` (one grid per run)
  so the app can turn a variant's `support_volume_voxels` into a cm³ estimate.

All scalar/POD — no core types cross the Swift importer boundary (header contract).
I did NOT bridge the variant mesh geometry array or the per-voxel von Mises field
array this run — those are consumed only by the deferred Metal render pass, so they
belong to that task (see "What I did NOT do"); bridging them now would be unused
plumbing.

### Swift wrapper — `Sources/TopOptKit/TopOptKit.swift`
`OptimizeVariant` and `OptimizeOutcome` gain the matching fields (`orientation:
SIMD3<Double>`, `maxStressMPa`, `maxInterlayerTensionMPa`, `inPlaneMargin`,
`interlayerMargin`; `voxelVolumeMM3`). Additive with defaulted inits (no existing
call site changed); the bridge→Swift mapping in `minimizePlastic` copies them.

### Tested logic — `Sources/TopOptFlows/ResultsModel.swift` (new)
The results-screen logic, exercised headlessly:
- `ResultVariantVM` (precomputed, `Equatable`): savings % + label ("−30%", U+2212),
  mass label ("199 g"/"1.24 kg"), support cm³ + label, tilt-from-vertical degrees,
  the recommended-orientation sentence (NO print time), `LayerShear`, max stress,
  worst-case margin, and the M5.2b min-feature advisory. `subLabel(active:)` gives
  "199 g · selected|plastic" (design).
- `LayerShear` (low/moderate/high) classified from the interlayer margin (safety
  factor). Thresholds are a documented PRESENTATION heuristic (the physics gate is
  the accepted/rejected margin, untouched), tuneable without core changes.
- `ResultsModel` (`@MainActor ObservableObject`): `tabs` (accepted variants only),
  `stressScaleMaxMPa` (shared scale = max von Mises across variants), selection
  (`selectedIndex` defaults to the middle-ish, design `resultTab: 1`), `stressOn`,
  and the morph scrub state (`playT`/`playing`/`scrub`/`togglePlay`/`advance`).
- `stressColor(fraction:)` — the design's blue→cyan→green→yellow→red gradient as a
  pure, clamped ramp; `stressFraction(mpa:)` maps a value onto the shared scale.
  (Ready for the Metal pass to color vertices; nothing renders it yet.)

### Thin UI (device QA) — `Sources/TopOptFlows/ResultsScreen.swift` (new)
The design's RESULTS chrome drawn over the 3D stage: back + project name +
"Optimized ✓"; the Stress toggle (gradient dot); Export .3mf button (visual now,
its real sheet is M7.9 — wired to a "arrives in M7.9" toast); the savings folder
tabs; the morph media player (play/scrub, driving `ResultsModel` state); and the
orientation cube → "Recommended orientation" sheet (summary + Layer shear, print
time omitted, plus the M5.2b advisory when a variant has thin features).

### Wiring — `Sources/TopOptFlows/WorkspacePlaceholder.swift`
When `run.phase == .succeeded` (RunModel already parks the `OptimizeOutcome`),
`ResultsScreen` overlays the workspace; Close → `run.reset()` returns to the
interactive workspace.

## Test evidence (raw, pasted, unedited)

### App package — `xcodebuild test -scheme TopOptKit-Package -destination 'platform=macOS'`
(OCCT-free-iOS manifest — git-ignored `occt-frameworks.generated.json` moved aside
+ SwiftPM manifest cache purged, the maintainer's documented macOS-test step;
benign `ld: warning … newer version` OCCT lines filtered, as in runs 034–044)
```
Test Suite 'TopOptDesignTests.xctest' passed at 2026-07-10 19:56:51.947.
	 Executed 17 tests, with 0 failures (0 unexpected) in 0.010 (0.017) seconds
Test Suite 'TopOptFlowsTests.xctest' passed at 2026-07-10 19:57:43.286.
	 Executed 157 tests, with 0 failures (0 unexpected) in 50.606 (50.667) seconds
Test Suite 'TopOptKitTests.xctest' passed at 2026-07-10 19:57:45.181.
	 Executed 20 tests, with 0 failures (0 unexpected) in 1.346 (1.351) seconds
** TEST SUCCEEDED **
```
(194 = 17 design + 157 flows [139 baseline + 18 ResultsModel] + 20 kit [19 + 1].)
Both iOS shipping slices build (OCCT-on default manifest):
- iOS Simulator: `xcodebuild build -project TopOpt.xcodeproj -scheme TopOpt -sdk
  iphonesimulator -destination 'generic/platform=iOS Simulator'
  CODE_SIGNING_ALLOWED=NO` → `** BUILD SUCCEEDED **`.
- iOS device: `… -sdk iphoneos -destination 'generic/platform=iOS'
  CODE_SIGNING_ALLOWED=NO` → `** BUILD SUCCEEDED **`.

### Core `ctest` — full suite (pre-flight baseline; no `/core/` file touched)
```
25/26 Test #25: mask_face ........................   Passed    0.06 sec
      Start 26: cli_demo
26/26 Test #26: cli_demo .........................   Passed  419.42 sec

100% tests passed, 0 tests failed out of 26

Total Test time (real) = 598.29 sec
```

CI run: <maintainer fills — /app/ is not on Linux CI; the /app/ gate is
`xcodebuild test` on the maintainer's Mac + device QA per ROADMAP M7 rules>
PR: <maintainer fills>

New tests added:
- `TopOptKitTests.testMinimizePlasticResultsFields` (real bridge on cube_10mm at
  res 6): `voxelVolumeMM3 > 0`; each variant's orientation is a real (unit)
  direction + finite; peak stresses finite/≥0; margins > 0 and
  `worstCaseMargin == min(inPlaneMargin, interlayerMargin)` (the locked definition
  — proves all three carry real, consistent numbers, not stubs).
- `ResultsModelTests` (18): savings/mass labels + sub-line; mass formatting
  (g/kg); accepted-only tabs; default-selected (middle vs single); select resets
  scrub + clamps; scrub clamps; play restarts at end + stops at 1; stress toggle;
  support cm³ conversion + "minimal"; tilt-from-vertical (upright, ±Z equal, 45°);
  orientation copy omits any print-time duration (regex-guarded); layer-shear
  boundaries (2.0 / 1.25 / ∞); stress ramp endpoints/midpoint + clamp; shared
  stress scale = max across variants.

### Test-honesty check (throwaway, nothing committed)
Changed `stressColor`'s top stop `(255,70,50)` → `(0,0,0)` and ran
`-only-testing:TopOptFlowsTests/ResultsModelTests`:
```
	 Executed 18 tests, with 2 failures (0 unexpected) in 0.498 (0.503) seconds
** TEST FAILED **
```
(`testStressColorRampEndpointsAndMidpoint` + `testStressColorClamps` — the ramp is
asserted, not smoke.) Restored from backup (grep confirmed the real stop back),
re-ran the full macOS suite: green (194, above).

## What I did NOT do
- **Did NOT check the M7.8 box.** This is the foundation half; the Metal render
  pass is the remaining half (see below). M7.8 stays `[ ]`.
- **Did NOT wire the Metal viewer to the variant.** The stage still renders the
  IMPORTED mesh behind the results chrome — the variant mesh is not yet displayed,
  stress is not vertex-colored, and the morph scrub drives `ResultsModel.playT` but
  does not yet morph geometry. That is the follow-up. Consequently I did NOT bridge
  the variant mesh-geometry array or the per-voxel von Mises field array (their only
  consumer is that render pass); the follow-up adds them to the bridge with shape/
  consistency tests, then samples the field per vertex against the shared scale
  (`ResultsModel.stressFraction` + `stressColor` are ready).
- **Did NOT implement Export (.3mf) or the export sheet** — that is M7.9. The button
  exists per the design and toasts a placeholder; `onExport` is the seam.
- **Did NOT implement "Save video"** from the design's media player (peripheral; not
  called out in the M7.8 task text).
- **Did NOT thread the user's M7.6 load case into the solve** — unchanged from
  handoff 044 (self-weight only; the tagged anchors/loads still don't steer the
  optimize). The results therefore reflect the self-weight run.
- **Did NOT verify any pixel on device.** The chrome, tabs, orientation sheet,
  scrub, and stress toggle are compiled + logic-tested but visually unverified
  (device QA, the M7 /app/ gate).
- **Did NOT persist results state** — the `ResultsModel` is per-success (rebuilt
  from `run.outcome`); Close resets the run and returns to the workspace.

## Warnings for the next run (the Metal follow-up)
- **The results box is UNCHECKED on purpose.** The follow-up (a) bridges the variant
  mesh + grid-indexed von Mises field + grid metadata, (b) swaps the workspace stage
  to render the selected variant's mesh, (c) colors vertices by
  `ResultsModel.stressFraction`→`stressColor` when `stressOn`, and (d) morphs the
  isosurface across `playT`. Only then check M7.8.
- **`ResultsModel` is the seam.** It already exposes `selectedIndex`/`selected`,
  `stressOn`, `playT`, `stressScaleMaxMPa`, and the color ramp — the render pass
  reads these; no new state should be needed on the model.
- **Layer-shear thresholds (2.0 / 1.25) are a UI heuristic**, not a physics gate.
  Tune in `LayerShear.classify` if device QA wants different bands; don't confuse
  them with the accepted/rejected margin (1.5×, core-owned).
- **Concurrent maintainer edits are in the working tree, unstaged and NOT part of my
  commit:** `docs/DECISIONS.md` (the 2026-07-11 print-time (b) entry) and
  `docs/ROADMAP.md` (the maintainer removed a stray `>>>>>>> Stashed changes`
  conflict marker that had been committed above the M7.7 line). I committed only my
  own files; those two remain for the maintainer to commit.
- Standard app-build caveats unchanged (runs 039–044): `./app/scripts/build_core.sh`
  only if `vendor/TopOptCore.xcframework` is stale (it was current — core untouched);
  the macOS test path needs the git-ignored manifest moved aside + SwiftPM cache
  purged; `Package.swift` stays committed-clean.

## Blocked
None. (The M7.8 print-time precondition that blocked the start was resolved by the
maintainer's DECISIONS.md 2026-07-11 (b) entry.)
